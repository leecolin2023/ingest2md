[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ingest2md"
version = "0.7.0"
description = "Turn web links, sharing text and local media into lightweight LLM-readable Markdown."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "beautifulsoup4",
  "camoufox",
  "httpx",
  "markdownify",
  "playwright",
  "PyYAML",
  "requests",
  "trafilatura",
  "yt-dlp",
]

[project.optional-dependencies]
test = ["pytest>=8"]
documents = ["markitdown[pdf,docx,pptx,xlsx]"]

[project.scripts]
ingest2md = "ingest2md.cli:main"

[tool.setuptools.packages.find]
include = ["ingest2md*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
